import { LoginColorDirective } from './login-color.directive';

describe('LoginColorDirective', () => {
  it('should create an instance', () => {
    const directive = new LoginColorDirective();
    expect(directive).toBeTruthy();
  });
});
